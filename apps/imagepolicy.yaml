apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: gitops-app
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: gitops-app
  policy:
    semver:
      range: ">=1.0.0"
